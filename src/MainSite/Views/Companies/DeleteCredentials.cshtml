@{
    ViewData["Title"] = "Delete Credentials";
}

<h3>Are you sure you want to delete this?</h3>
<form asp-action="DeleteCredentials">
    <div class="form-actions no-color">
        <input type="hidden" hidden value="@ViewBag.cid" name="cid"/>
        <input type="submit" value="Delete" class="btn btn-danger"/> |
        <a asp-action="Details" asp-route-cid="@ViewBag.compId">Back to List</a>
    </div>
</form>